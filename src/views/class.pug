extends default

block content
  h1 #{group.name}班课程内容

  if lessons.length
    ul
      each lesson in lessons
        - const m = moment(lesson.date)
        - const lessonDate = new Date(lesson.date)
        - 
          const calDate = m.calendar(null, {
            sameDay : '[今天]',
            nextDay : '[明天]',
            nextWeek : '[下]dddd',
            lastDay : '[昨天]',
            lastWeek : '[上]dddd'
          })  
        li.links
          a(href=`/summary/${lesson._id}`) 
            h4(style="margin-bottom:0;") 第#{lesson.number}课 - #{lesson.title}
            p(style="font-size: 1.6rem;")= calDate

  else
    p 还没有输入这个班级的课程内容。